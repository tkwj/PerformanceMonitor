<Window x:Class="PerformanceMonitorDashboard.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Performance Monitor Dashboard"
        Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Performance Monitor Dashboard" FontWeight="Bold" FontSize="18" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="1" x:Name="VersionText" Text="Version 1.2.0" FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}" Margin="0,0,0,20"/>

        <!-- Copyright -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="Copyright (c) 2026 Darling Data, LLC" FontSize="12"/>
            <TextBlock Text="Licensed under the MIT License" FontSize="12" Foreground="{DynamicResource ForegroundMutedBrush}"/>
        </StackPanel>

        <!-- Links -->
        <StackPanel Grid.Row="3" Margin="0,0,0,20">
            <TextBlock FontSize="12" Margin="0,0,0,5">
                <Hyperlink x:Name="GitHubLink" Click="GitHubLink_Click">
                    GitHub Repository
                </Hyperlink>
            </TextBlock>
            <TextBlock FontSize="12" Margin="0,0,0,5">
                <Hyperlink x:Name="ReportIssueLink" Click="ReportIssueLink_Click">
                    Report an Issue
                </Hyperlink>
            </TextBlock>
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <Button Content="Check for Updates" Click="CheckUpdatesLink_Click" Padding="10,4" FontSize="12"/>
                <TextBlock x:Name="UpdateStatusText" FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0"
                           Foreground="{DynamicResource ForegroundMutedBrush}" Visibility="Collapsed"/>
            </StackPanel>
        </StackPanel>

        <!-- Links -->
        <StackPanel Grid.Row="4">
            <TextBlock FontSize="12">
                <Hyperlink x:Name="DarlingDataLink" Click="DarlingDataLink_Click">
                    www.erikdarling.com
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <!-- Close Button -->
        <Button Grid.Row="5" Content="Close" Width="75" HorizontalAlignment="Right" Click="CloseButton_Click" IsDefault="True" IsCancel="True"/>
    </Grid>
</Window>
